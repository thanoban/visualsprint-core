"""Capture Mode A2 — official platform artifacts (Meet REST, Zoom Cloud Recording).

No bot, no DOM scraping. We take the platforms' audio + speaker labels only, never
their transcripts (see docs/03-capture.md) — code-switching transcription is our job.
"""
